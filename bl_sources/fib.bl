function fib(int n) -> int {
    if (n == 1 || n == 2) {
        return 1;
    }

    int result = fib(n-1) + fib(n-2);

    return result;
}

function main() -> int {
    for (int i = 1; i <= 100; i = i + 1) {
        print(fib(i));
    }
    return 0;
}
